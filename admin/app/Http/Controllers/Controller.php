<?php

namespace App\Http\Controllers;

/**
 * Base controller (matches the Laravel 11 skeleton). Present so these files work
 * even if copied into a bare app.
 */
abstract class Controller
{
    //
}
